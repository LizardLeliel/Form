#include "argparse.h"
//#include "lex.yy.c"
/*
int argParse(int argc, char** argv) {
    if (argc < 2) {
        printf("Invalid arguements. Format:\n");
        printf("%s [input file]\n", argv[0]);
        return -1;
    }
    else {
        yyin = fopen(argv[1], "r");
        printf("File opened: %s\n", argv[1]);
        int n = yylex();
        return n;
    }
}
*/
