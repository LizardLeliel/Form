#ifndef TEST_HEADER

/* This header defines one function which runs a bunch of sanity checks.
 *  More in-depth analysis will be written later; Although for now, it
 *  it just makes sure intermediate steps aren't doing weird things until
 *  everything's tied together.
 *
 * It is expected this file and test.c will be removed from the git repository
 *  once things become stable
 */

// void runChecks();

#define TEST_HEADER
#endif
